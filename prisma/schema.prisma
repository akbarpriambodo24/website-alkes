generator client {
  provider = "prisma-client-js"
}

datasource db {
  provider = "sqlite"
}

model Product {
  id          Int      @id @default(autoincrement())
  name        String
  category    String
  price       String
  description String?
  image       String?
  createdAt   DateTime @default(now())
}